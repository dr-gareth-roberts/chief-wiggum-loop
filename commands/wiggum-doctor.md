---
description: "Run Wiggum preflight diagnostics"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/wiggum-doctor.sh)"]
---

# Wiggum Doctor

Run:

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/wiggum-doctor.sh"
```

Report each check as ✅/❌ and exit non-zero if anything is broken.
