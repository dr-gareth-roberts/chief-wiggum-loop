---
description: "Cancel the active Wiggum Loop"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel-wiggum-loop.sh)"]
---

# Cancel Wiggum Loop

Run:

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-wiggum-loop.sh"
```

Report whether a loop was cancelled and where the archived state was written.
